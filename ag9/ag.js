/* Adding Comments: Choose two of the programs you’ve written,
 and add at least one comment to each.
 If you don’t have anything specific to write because
 your programs are too simple at this point, just add
 your name and the current date at the top of each program file.
  Then write one sentence describing what the program does. */
// program for multiplication table of 9
var table = 9;
console.log('multiplication table of 9');
for (var num = 0; num < 11; num++) {
    console.log(table + ' x ' + num + ' = ' + (table * num));
}
// Today date and time
console.log(Date());
